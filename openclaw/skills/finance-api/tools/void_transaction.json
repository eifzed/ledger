{
  "name": "void_transaction",
  "description": "Void (cancel) a posted transaction. The transaction's status changes to 'voided' and its effect on account balances is reversed. This is irreversible.",
  "parameters": {
    "type": "object",
    "properties": {
      "txn_id": {
        "type": "integer",
        "description": "Auto-incrementing integer ID of the transaction to void (e.g. 1, 2, 42)."
      }
    },
    "required": [
      "txn_id"
    ]
  }
}
